# LogSight
BloomLog – In-Memory High-Performance Log Service A Spring Boot web application for fast, in-memory log ingestion and querying. Uses SHA-256 hashing and Bloom filters for efficient exact-match searches, supports substring queries, and automatically cleans up old logs daily. No database required.
